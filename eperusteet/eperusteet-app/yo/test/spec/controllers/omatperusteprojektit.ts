'use strict';

describe('Controller: OmatperusteprojektitCtrl', function () {

  // load the controller's module
  beforeEach(module('eperusteApp'));

  var OmatperusteprojektitCtrl,
    scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    OmatperusteprojektitCtrl = $controller('OmatperusteprojektitCtrl', {
      $scope: scope
    });
  }));

  it('should work', function () {
    //TODO
  });
});
