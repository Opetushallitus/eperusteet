'use strict';

describe('Directive: dateformatvalidator', function () {

  // load the directive's module
  beforeEach(module('eperusteApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should work', inject(function ($compile) {
    //TODO
  }));
});
