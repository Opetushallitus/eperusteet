alter table peruste
        add luoja varchar(255),
        add luotu timestamp,
        add muokattu timestamp,
        add muokkaaja varchar(255);

alter table peruste_AUD
        add luoja varchar(255),
        add luotu timestamp,
        add muokattu timestamp,
        add muokkaaja varchar(255);
