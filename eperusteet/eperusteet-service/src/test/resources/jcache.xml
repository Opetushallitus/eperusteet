<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xmlns:jsr107="http://www.ehcache.org/v3/jsr107"
        xsi:schemaLocation="
            http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd
            http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.0.xsd">

    <cache alias="validointivirheet">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">50</heap>
        </resources>
    </cache>
    <cache alias="tila" >
        <expiry>
            <ttl unit="seconds">3600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>
    <cache alias="koulutusalat">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10</heap>
        </resources>
    </cache>
    <cache alias="koodistokoodit">
        <expiry>
            <ttl unit="seconds">3600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>
    <cache alias="koodistot">
        <expiry>
            <ttl unit="seconds">6000</ttl>
        </expiry>

        <resources>
            <heap unit="entries">20</heap>
        </resources>
    </cache>
    <cache alias="opintoalat">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10</heap>
        </resources>
    </cache>
    <cache alias="kayttajat">
        <expiry>
            <ttl unit="seconds">3600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1000</heap>
        </resources>
    </cache>
    <cache alias="kayttajan_projekti">
        <expiry>
            <ttl unit="seconds">3600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>
    <cache alias="peruste-navigation">
        <expiry>
            <ttl unit="seconds">3600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">500</heap>
        </resources>
    </cache>
    <cache alias="lokalisoinnit">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1000</heap>
        </resources>
    </cache>
    <cache alias="kategorialokalisoinnit">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1000</heap>
        </resources>
    </cache>
    <cache alias="pohjaperusteet">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1000</heap>
        </resources>
    </cache>
    <cache alias="julkaistutkoulutustyypit">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">3</heap>
        </resources>
    </cache>
    <cache alias="amosaatilastot">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1</heap>
        </resources>
    </cache>
    <cache alias="ylopstilastot">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1</heap>
        </resources>
    </cache>
    <cache alias="yllapitovalues">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">100</heap>
        </resources>
    </cache>

    <cache alias="maarayskokoelma_asiasanat">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1</heap>
        </resources>
    </cache>

    <cache alias="julkinenEtusivu">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1</heap>
        </resources>
    </cache>

    <cache alias="julkinen_peruste_navigointi">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1000</heap>
        </resources>
    </cache>

</config>